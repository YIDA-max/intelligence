# 物流费用（前端模块）

## 目录说明

| 路径                                                     | 说明                                  |
| -------------------------------------------------------- | ------------------------------------- |
| `src/views/materialFlow/logisticsFee/lastMileFee/`       | 尾程费用列表与录入                    |
| `src/api/materialFlow/logisticsFee/lastMileFee/index.ts` | 尾程费用 API（占位，待后端 URL 对齐） |

## 动态路由（后端菜单）

菜单由 `/admin/menu` 下发，前端需在后台配置：

- **componentPath**：`/materialFlow/logisticsFee/lastMileFee/index`

## 命名约定

- 模块英文名：`logisticsFee`（与 `integrations/logisticsCostIntegration` 区分：后者偏集成与金蝶单据）
- 尾程页面目录：`lastMileFee`

## 后续规划

- 头程费用优化（PRD 4.4）见各业务页；本目录后续可继续收纳「头程/尾程」统一组件（可选）。

## 关联文档

- [尾程费用.md](./lastMileFee/尾程费用.md)
