<!--
 * @Author: wuyueqin wuyueqin@merach.com
 * @Date: 2026-01-31 16:08:09
 * @LastEditors: wuyueqin wuyueqin@merach.com
 * @LastEditTime: 2026-02-13 10:05:54
 * @FilePath: \qianyi-uie:\过程md\金蝶、\生成脚本.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->

<!--
 * @Description:
-->

## 金蝶集成报表生成脚本

@金蝶.apifox.json 这个是 apiFox 导出的接口文档 apifox 模式，请结合导出的特点生成对应的 api 文档，例如@金蝶.apifox.json (218-220) 这个是模块名称下面自己放着该模块的分页查询、新增、修改、日志等接口后面的模块基本都是这样，@金蝶.apifox.json (255-259) 这个 name 是接口名称，path 是接口路径，"query"是查询入参，"responses": 接口返回的数据，大致模式是这样，

1. 现在根据模块名称在目录 src\api\integrations 下生成文件夹，文件夹下放 index，示例采购组织关系@src/api/integrations/baseAssociationConfig/index.ts ，
2. 文件夹命名规则这个 src\views\integrations\baseAssociationConfig 路径下的各个文件夹下的 md 对应，例如采购组织关系表对应 src\views\integrations\baseAssociationConfig\purchaseOrgRelation\采购组织关系.md，api 的模块名称叫 purchaseOrgRelation 放在 src\api\integrations\baseAssociationConfig\index.ts 下，如果匹配不到的统一视为公共接口放在 E:\mrk\qianyi-ui\src\api\integrations\common 下
3. 生成接口文档并且对应的 ts 类型定义
4. 记住这些前置条件，接下来我会将这些模块逐个放到.vue 文件中使用接口的相关配置
5. 生成的接口都需冗余出入参，例如分页查询、新增、修改、删除等接口都需冗余出入参

## 补充优化

1. 生成的导出接口没带入参
   @src/api/integrations/baseAssociationConfig/index.ts 优化导出的相关接口参考配置使用方式@index.ts (23-32)
2. 所有数据都生成在了一个文件下

@index.ts (804-925) 将这样一个模块的拆分出去做一个文件夹，放 inventoryOrgRelation 下的 index 中,现在所在的目录是采购组织关系，请按这个模式将其他的定义也拆分成文件夹夹@src/views/integrations/baseAssociationConfig 文件夹名称定义参考这个文件夹

## 优化后的脚本 2.0

@金蝶.apifox.json 这个是 apiFox 导出的接口文档 apifox 模式，请结合导出的特点生成对应的 api 文档，例如 apiCollection.items[0].name 这个是模块名称,apiCollection.items 下面自己放着该模块的涉及的接口，例如分页查询、新增、修改、日志等接口后面的模块基本都是这样，字段举例说明 apiCollection.items[0].items[0]下这个 name 是接口名称，path 是接口路径，"query"是查询入参，"responses": 接口返回的数据，大致模式是这样，

//已定义文件夹

1. 根据当前目录匹配对应的文件夹，将接口定义到对应的文件夹下，如果没有对应的文件夹，就创建一个
2. 生成接口文档并且对应的 ts 类型定义
3. 生成的接口都需冗余出入参，例如分页查询、新增、修改、删除等接口都需冗余出入参，保证以后接口增加出入参时，不会影响到已有的接口定义
4. 将定义好的接口替换到对应目录下，目录：

//生成文件夹开始

1. 现在根据模块名称在目录 src\api\integrations 下生成文件夹，文件夹下放 index，示例采购组织关系@src/api/integrations/baseAssociationConfig/index.ts ，
2. 文件夹命名规则这个 src\views\integrations\baseAssociationConfig 路径下的各个文件夹下的 md 对应，例如采购组织关系表对应 src\views\integrations\baseAssociationConfig\purchaseOrgRelation\采购组织关系.md，api 的模块名称叫 purchaseOrgRelation 放在 src\api\integrations\baseAssociationConfig\index.ts 下，如果匹配不到的统一视为公共接口放在 E:\mrk\qianyi-ui\src\api\integrations\common 下
3. 生成接口文档并且对应的 ts 类型定义
4. 记住这些前置条件，接下来我会将这些模块逐个放到.vue 文件中使用接口的相关配置
5. 生成的接口都需冗余出入参，例如分页查询、新增、修改、删除等接口都需冗余出入参

//已有文件进行处理

@金蝶.apifox.json 这个是 apiFox 导出的接口文档 apifox 模式，请结合导出的特点生成对应的 api 文档，例如 apiCollection.items[0].name 这个是模块名称,apiCollection.items 下面自己放着该模块的涉及的接口，例如分页查询、新增、修改、日志等接口后面的模块基本都是这样，字段举例说明 apiCollection.items[0].items[0]下这个 name 是接口名称，path 是接口路径，"query"是查询入参，"responses": 接口返回的数据，大致模式是这样

接口文档中仓间调拨（收货）金蝶单据 对应当前项目的【仓间调拨发货】

接口文档中仓间调拨（发货）金蝶单据 对应当前项目的【仓间调拨收货】

src 下的文件匹配路径，api 和 views 的路径是一致的

/src/api/一级模块/二级模块/三级模块（部分模块没有三级模块）/index

/src/views/一级模块/二级模块/三级模块（部分模块没有三级模块）/index

1. 需要实现的模块 仓间调拨发货 仓间调拨收货
2. 现在 api 接口是之前模拟定义，需要根据接口重新定义，现在 api 下的内容是示例
3. 生成接口文档并且对应的 ts 类型定义
4. 记住这些前置条件，接下来我会将这些模块逐个放到.vue 文件中使用接口的相关配置
5. 生成的接口都需冗余出入参，例如分页查询、新增、修改、删除等接口都需冗余出入参
6. 接下来我还要将定义好的接口引入 views 下的.vue 文件，

第二步

好的接下来还是这个文件夹，将查询字段进行对接 searchConfig 下是查询配置的字段，如果匹配不上的或者相近可能的

component: 'customCascaderApi'类型组件使用 text 匹配接口的查询字段对接到当前的 code 上

component: 'TreeSelectInputApi'类型组件使用 text 匹配接口的查询字段对接到当前的 code 上

component: 'SelectInput',类型组件使用 label 匹配接口的查询字段对接到当前的 value 上

component: 'SelectDate',类型组件使用 label 匹配接口的查询字段对接到当前的开始时间字段 dateStart 和结束时间字段 dateEnd 上，这个可能会字段模糊匹配具体偏差比较大的请指出

columns 是接口返回字段页面显示的配置，使用当前的 title 对接口返回字段进行匹配，匹配后字段填写到 field 中，如果匹配不到则汇总列出来给我
